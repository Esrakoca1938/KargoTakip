@model KargoTakip.WebUI.Models.KargoDetayDto

@{
    ViewData["Title"] = "Create";
    Layout = "AdminLayout";
}

<h1>Create</h1>

<h4>KargoDetay</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="KargoId" class="control-label"></label>
                <select asp-for="KargoId" class ="form-control" asp-items="ViewBag.Kargo"></select>
            </div>
            <div class="form-group">
                <label asp-for="SubeId" class="control-label"></label>
                <select asp-for="SubeId" class ="form-control" asp-items="ViewBag.Sube"></select>
            </div>
            <div class="form-group">
                <label asp-for="IslemTuruId" class="control-label"></label>
                <select asp-for="IslemTuruId" class ="form-control" asp-items="ViewBag.IslemTuru"></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
