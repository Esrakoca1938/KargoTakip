@model KargoTakip.WebUI.Models.KargoDetayDto

@{
    ViewData["Title"] = "Edit";
    Layout = "AdminLayout";
}

<h1>Edit</h1>

<h4>KargoDetay</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="KargoId" class="control-label"></label>
                <select asp-for="KargoId" class="form-control" asp-items="ViewBag.KargoId"></select>
                <span asp-validation-for="KargoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SubeId" class="control-label"></label>
                <select asp-for="SubeId" class="form-control" asp-items="ViewBag.SubeId"></select>
                <span asp-validation-for="SubeId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IslemTuruId" class="control-label"></label>
                <select asp-for="IslemTuruId" class="form-control" asp-items="ViewBag.IslemTuruId"></select>
                <span asp-validation-for="IslemTuruId" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
